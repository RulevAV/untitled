let browsers = ['Chrom','Firefox','Edge','Safari','Opera'];
//Перебираем ключи
for (let browser in browsers)
{
    console.log(browser);
}
//Перебираем значение
for (let browser of browsers)
{
    console.log(browser);
}
